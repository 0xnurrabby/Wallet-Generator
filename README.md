# BaseFlow Wallet Lab v2

Fix applied:
- 12 / 15 / 18 / 21 / 24 word phrases now generate from exact BIP-39 entropy sizes
- derivation now uses HDNodeWallet.fromPhrase(...)
- status text now shows the selected phrase length

If your browser still shows the old error, close the old tab and reopen this updated file.
